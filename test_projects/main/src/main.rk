> foo::Bar

infix 7 |>

macro my_macro
    $name:ident $($arg:ident)* =>
        $name = a -> a
        $($arg)*

%my_macro lol

struct MyStruct
    field : Int

enum MyEnum
    Foo Bar
    Baz

trait MyTrait
    foo : Bar -> Baz
    baz = a -> a

impl MyTrait
    baz = a -> a
    foo = a -> a

lambda = (*5)

main = ->
    foo a, b
    foo[a + b + -c]

    foo.bar.baz

    a = MyStruct
        field: 42

    a + a + c

    Foo::Bar baz

    for i in a
        a

    while a == b
        a = a + b

    loop
        foo bar

    if a
    then foo
    else bar

    if b
    then
        a b
        b c
    else
        c + d
        b c

    a = (.foo)

< MyTrait
